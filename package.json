{
  "name": "wallgraph",
  "version": "0.5.0",
  "private": true,
  "description": "A free, browser-based floorplan editor. mm-exact, wall-graph based, zero runtime dependencies.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "tsx scripts/build.ts --watch --serve",
    "build": "npm run typecheck && tsx scripts/build.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "pretest": "node scripts/check-tests.mjs",
    "test": "tsx --test --test-reporter=spec \"tests/**/*.test.ts\"",
    "check": "npm run typecheck && npm test",
    "assets:og": "tsx scripts/render-og.ts",
    "assets": "npm run assets:og && magick assets/icon.png -resize 32x32 -strip assets/favicon-32.png && magick assets/icon.png -resize 180x180 -background none -strip assets/apple-touch-icon.png && magick assets/icon.png -resize 192x192 -strip assets/icon-192.png && magick assets/icon.png -resize 512x512 -strip assets/icon-512.png && magick assets/icon.png -define icon:auto-resize=48,32,16 assets/favicon.ico",
    "check:bundle": "node scripts/check-selfcontained.mjs",
    "check:seo": "node scripts/check-seo.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.0",
    "web-ifc": "^0.0.77"
  }
}
